# Panel de staff — 05

Repositorio Git independiente de AI_Agency. Revisa aquí `git status` antes de
editar. React + TypeScript + Vite; comandos y rutas en `README.md`.

- Esta es la vista del equipo; la vista del cliente está en `../06-dashboard-web/`.
- Conserva los estilos y componentes existentes. Los botones comparten criterios
  con el dashboard; comprueba ambos usos si modificas un patrón compartido.
- El backend está en `../04-codigo/panel_config_api_lambda.py` y los contratos de
  frontend en `src/api.ts`. No deduzcas el tenant desde datos manipulables.
- Para cambios que dependan de producción, consulta `../docs/estado.md` y verifica
  el recurso pertinente. El estado de A3 ya no se toma del índice de memorias.
- No imprimas tokens ni credenciales y no arrastres cambios de otra sesión.
